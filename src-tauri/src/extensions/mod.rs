//! Extensions layer — optional, pluggable feature modules.
//! Each extension lives in its own subdirectory: extensions/<ExtensionName>/mod.rs
//! Extensions may depend on modules/ but must not depend on each other.
